# filter_rejects.R
# 2022-03-02

# Following SqCL pipeline workflow step one deduplication and filter
# We identified several samples that doesn't seem to 'work', that is there
# were no outputs as R1_bb.final.fq.gz files. These are then supposed to be 